services:
    _defaults:
        autowire: true
        autoconfigure: true

    # Auto-register all services in the bundle
    LarsVanDerSangen\ProjectTemplateBundle\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/ProjectTemplateBundle.php'
            - '../src/User/Entity/'
            - '../src/User/Enum/'

    # Controllers
    LarsVanDerSangen\ProjectTemplateBundle\Api\Controller\:
        resource: '../src/Api/Controller/'
        tags: ['controller.service_arguments']

    LarsVanDerSangen\ProjectTemplateBundle\Auth\Controller\:
        resource: '../src/Auth/Controller/'
        tags: ['controller.service_arguments']

    LarsVanDerSangen\ProjectTemplateBundle\User\Controller\:
        resource: '../src/User/Controller/'
        tags: ['controller.service_arguments']

    # Mail Service with configurable sender
    LarsVanDerSangen\ProjectTemplateBundle\Shared\Service\MailService:
        arguments:
            $senderEmail: '%project_template.mailer_sender%'

    # Custom environment variable processor for Git branch-based database naming
    LarsVanDerSangen\ProjectTemplateBundle\Shared\DependencyInjection\GitBranchEnvVarProcessor:
        tags: ['container.env_var_processor']

    # Database copy command (only for dev/test environments)
    LarsVanDerSangen\ProjectTemplateBundle\Shared\Command\DatabaseCopyCommand:
        arguments:
            $environment: '%kernel.environment%'
            $databaseUrl: '%env(DATABASE_URL)%'

    # Master data loader service
    LarsVanDerSangen\ProjectTemplateBundle\Shared\Service\MasterDataLoader:
        arguments:
            $projectDir: '%kernel.project_dir%'
            $environment: '%kernel.environment%'

    # Master data services
    LarsVanDerSangen\ProjectTemplateBundle\Shared\Service\EntityReferenceRegistry:
        # Singleton service to track entity references

    LarsVanDerSangen\ProjectTemplateBundle\Shared\Service\EntityDependencyResolver:
        arguments:
            $entityManager: '@doctrine.orm.entity_manager'

    LarsVanDerSangen\ProjectTemplateBundle\Shared\Service\MasterDataHydrator:
        arguments:
            $objectManager: '@doctrine.orm.entity_manager'
            $referenceRegistry: '@LarsVanDerSangen\ProjectTemplateBundle\Shared\Service\EntityReferenceRegistry'
            $passwordHasher: '@security.user_password_hasher'

