# Import bundle routes (same as app: type service so routes always load)
project_template_bundle:
    resource: 'VanDerSangen\ProjectTemplateBundle\Routing\ProjectTemplateRouteLoader::loadRoutes'
    type: service
    prefix: /

